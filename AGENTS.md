# 项目开发约定（暂行）

本文件适用于当前前端项目及其子目录。以下约定根据用户逐项确认的意见整理；尚未确认的事项不要自行扩展为强制规范。

## 项目背景与结构

- 项目名称：孤独市集，面向校园闲置交易与线下邀约。
- 技术栈：Vue 3、Vue Router、PrimeVue、Tailwind CSS、Vite，使用 npm 管理依赖。
- `src/views/`：路由页面；`src/components/`：复用组件。
- `src/router/index.ts`：路由配置；`src/main.ts`：应用初始化及 PrimeVue 主题配置。
- `src/assets/css/`：全局样式；`src/assets/fonts/`：现有字体资源。
- 阅读相关代码后再修改，遵循所在文件已有的代码风格与组织方式。
- 新增或修改注释时使用中文，重点解释复杂逻辑和设计原因，避免重复代码本身已清楚表达的含义。

## 技术选型

- 沿用现有技术栈，优先使用 PrimeVue 组件并复用项目已有组件。
- 新增 Vue 组件使用 TypeScript 和 `<script setup lang="ts">`；已有文件保持原有语言和写法，不主动迁移。
- 添加任何新依赖之前，先向用户说明用途并获得同意。
- 不因个人偏好替换现有框架、组件库或样式方案。

## UI 与交互

- 优先复用项目现有素材和已有图标库；引入外部素材或生成图片前，先询问用户并获得确认。
- 默认使用中文文案，为输入框提供明确关联的标签，确保交互可通过键盘操作，并适配移动端布局。
- 遵循项目现有的简约黑白风格，延续已有主题、字体及页面布局习惯。
- 较大的 UI 改动先说明方案并与用户确认；局部小调整可以直接完成。
- 未明确的小细节按现有代码和惯例处理；涉及功能行为或较大改动时先询问用户。

## 接口尚未提供时

- 可以先完成 UI 和本地交互，并明确提示相关功能尚未接入接口。
- 不模拟登录成功、提交成功或其他依赖服务端确认的成功结果。
- 接口方案先询问用户，再开展对应实现。

## 修改范围与协作

- 开始任务时说明做法，遇到问题或重要变化时更新进度，完成后简要汇报结果。
- 仅在用户明确要求时，才使用子 Agent 协助开发。
- 用户已明确要求创建某个文件或拆分组件时，视为已授权，直接执行，不重复确认；由 Agent 自行提出的新增文件或组件拆分，必须先询问用户并获得确认。
- 只修改当前任务涉及的内容，不主动进行无关重构或清理。
- 发现与当前任务无关的问题时，告知用户，由用户决定是否处理。
- 保留用户已有的修改，不覆盖或回退无关工作。
- 对需要确认的事项提出具体问题；已经确认的约定直接执行，不重复征求同意。

## 验证与交付

- 修改影响代码或构建配置时，完成后在项目根目录执行 `npm run build`。
- 仅修改文档或注释且不影响运行时，免运行构建。
- 默认仅运行构建，其余由用户手动验收；用户另行要求时再进行额外验证。
- 如构建失败，说明原因；只修复当前任务范围内的问题，无关问题交由用户决定。
- 交付时简要说明改动内容、构建结果及未接入接口等实际限制。
- 不把构建通过表述为浏览器效果、类型检查或业务流程已经验证通过。

## Git 与提交信息

- 密钥、令牌和密码不写入源码或提交到 Git；配置示例使用占位值。
- 可以主动查看 Git 状态和差异。
- 提交、推送、切换分支及其他改变 Git 状态的操作，必须由用户明确要求后才能执行。
- 提交信息采用 Conventional Commits，格式为 `type(scope): 中文说明`，其中 scope 可选。
- 类型按实际变更选择，例如 `feat`、`fix`、`docs`、`style`、`refactor`、`test`、`chore`。
- 具体 commit message 由用户编辑，Agent 不代写、不改写。
- 用户要求提交但尚未提供 message 时，先请用户提供。若 message 不符合约定，指出问题并请用户自行修改。
